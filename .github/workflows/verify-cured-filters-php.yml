name: Verify cured filters PHP

on:
  push:
    branches:
      - feat/embutidos-curados-filters

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint changed PHP
        run: |
          php -l mdo-supplier-sync/includes/class-mdo-cured-producer.php
          php -l mdo-supplier-sync/mdo-supplier-sync.php
          php -l elmercadodeorigen-child/inc/category-specific-filters-010185.php
