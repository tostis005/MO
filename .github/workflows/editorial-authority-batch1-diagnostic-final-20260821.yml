name: Historical editorial diagnostic disabled

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Historical diagnostic intentionally disabled for automatic pushes.'
