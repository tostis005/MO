name: Verify empty related guides cheese

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate_authority_gaps:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Build and validate complete authority gaps package
        run: python .github/scripts/build-authority-gaps-package-20260902.py /tmp/authority-gaps-all.json
