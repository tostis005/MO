name: Verify empty related guides cheese

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose_authority_gaps:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Diagnose authority gaps editorial package
        run: python .github/scripts/diagnose-authority-gaps-payload-20260902.py
