name: Request 010201 filter coherence smoke

on:
  push:
    paths:
      - 'elmercadodeorigen-child/functions.php'
      - 'elmercadodeorigen-child/inc/catalog-filter-visual-lock-010200.php'
      - '.github/scripts/request-010201-filter-coherence-smoke.cjs'
      - '.github/workflows/request-010201-filter-coherence-smoke.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install browser dependency
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y google-chrome-stable
          npm install --no-save --no-package-lock puppeteer-core@24 >/dev/null 2>&1
      - name: Validate smoke syntax
        run: node --check .github/scripts/request-010201-filter-coherence-smoke.cjs
      - name: Review unified filter panel on staging
        run: node .github/scripts/request-010201-filter-coherence-smoke.cjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: request-010201-filter-coherence-smoke
          path: qa/
          retention-days: 14
