name: Request 010203 filter system smoke

on:
  push:
    paths:
      - 'elmercadodeorigen-child/functions.php'
      - 'elmercadodeorigen-child/inc/catalog-filter-system-010203.php'
      - '.github/scripts/request-010203-filter-system-smoke.cjs'
      - '.github/workflows/request-010203-filter-system-smoke.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install browser dependency
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y google-chrome-stable
          npm install --no-save --no-package-lock puppeteer-core@24 >/dev/null 2>&1
      - name: Validate smoke syntax
        run: node --check .github/scripts/request-010203-filter-system-smoke.cjs
      - name: Review single filter system on staging
        run: node .github/scripts/request-010203-filter-system-smoke.cjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: request-010203-filter-system-smoke
          path: qa/
          retention-days: 14
