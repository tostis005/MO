name: Historical home CSS workflow disabled

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Historical workflow intentionally disabled for automatic pushes.'
