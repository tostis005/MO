name: Legacy alternate oil meat landing deploy - disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Disabled to prevent SEO landing duplication
        run: echo 'This legacy alternate landing workflow is intentionally disabled. Use deploy-aceites-carnes-landings-production-20260830.yml instead.'
