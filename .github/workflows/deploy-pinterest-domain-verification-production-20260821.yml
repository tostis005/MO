name: Deploy Pinterest domain verification

on:
  push:
    branches: [main]
    paths:
      - 'mu-plugins/mdo-pinterest-domain-verification.php'
      - '.github/workflows/deploy-pinterest-domain-verification-production-20260821.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      STAGING_HOST: ${{ secrets.STAGING_HOST }}
      STAGING_PORT: ${{ secrets.STAGING_PORT }}
      STAGING_USER: ${{ secrets.STAGING_USER }}
      STAGING_PASSWORD: ${{ secrets.STAGING_PASSWORD }}

    steps:
      - uses: actions/checkout@v4

      - name: Validate PHP
        run: php -l mu-plugins/mdo-pinterest-domain-verification.php

      - name: Deploy verification tag to production
        shell: bash
        env:
          SSHPASS: ${{ secrets.STAGING_PASSWORD }}
        run: |
          set -euo pipefail
          : "${STAGING_HOST:?}"; : "${STAGING_USER:?}"; : "${STAGING_PASSWORD:?}"

          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends sshpass >/dev/null

          PORT="${STAGING_PORT:-22}"
          SSH_OPTIONS="-o BatchMode=no -o PreferredAuthentications=password,keyboard-interactive -o PubkeyAuthentication=no -o StrictHostKeyChecking=no -p $PORT"
          SCP_OPTIONS="-o BatchMode=no -o PreferredAuthentications=password,keyboard-interactive -o PubkeyAuthentication=no -o StrictHostKeyChecking=no -P $PORT"

          PROD_PATH="$(sshpass -e ssh $SSH_OPTIONS "$STAGING_USER@$STAGING_HOST" '
            wp_bin=$(command -v wp || true)
            php_bin=$(command -v php || true)
            [ -n "$php_bin" ] || php_bin=$(find /opt/plesk/php -maxdepth 3 -type f -path "*/bin/php" 2>/dev/null | sort -Vr | head -n1)
            for cfg in $(find /var/www/vhosts "$HOME" -maxdepth 7 -type f -path "*/httpdocs/wp-config.php" 2>/dev/null); do
              p=$(dirname "$cfg")
              url=$("$php_bin" -d memory_limit=256M "$wp_bin" option get siteurl --path="$p" --skip-plugins --skip-themes --allow-root 2>/dev/null || true)
              case "$url" in
                https://www.elmercadodeorigen.com|http://www.elmercadodeorigen.com|https://elmercadodeorigen.com|http://elmercadodeorigen.com) printf "%s" "$p"; break ;;
              esac
            done
          ')"
          : "${PROD_PATH:?production path not found}"

          REMOTE_FILE="/tmp/mdo-pinterest-domain-verification-${GITHUB_RUN_ID}.php"
          sshpass -e scp $SCP_OPTIONS mu-plugins/mdo-pinterest-domain-verification.php "$STAGING_USER@$STAGING_HOST:$REMOTE_FILE"

          sshpass -e ssh $SSH_OPTIONS "$STAGING_USER@$STAGING_HOST" "
            set -e
            TARGET='$PROD_PATH/wp-content/mu-plugins/mdo-pinterest-domain-verification.php'
            mkdir -p '$PROD_PATH/wp-content/mu-plugins'
            if [ -f \"\$TARGET\" ]; then
              cp \"\$TARGET\" \"/tmp/mdo-pinterest-domain-verification-${GITHUB_RUN_ID}.bak.php\"
            fi
            cp '$REMOTE_FILE' \"\$TARGET\"
            rm -f '$REMOTE_FILE'

            php_bin=\$(command -v php || true)
            [ -n \"\$php_bin\" ] || php_bin=\$(find /opt/plesk/php -maxdepth 3 -type f -path '*/bin/php' 2>/dev/null | sort -Vr | head -n1)
            \"\$php_bin\" -l \"\$TARGET\" >/dev/null
            grep -Fq 'bd9b4a2c373b774bb7753fa7040172ac' \"\$TARGET\"

            wp_bin=\$(command -v wp || true)
            if [ -n \"\$wp_bin\" ]; then
              \"\$php_bin\" -d memory_limit=512M \"\$wp_bin\" cache flush --path='$PROD_PATH' --allow-root >/dev/null || true
            fi
          "

      - name: Verify tag on public site
        shell: bash
        run: |
          set -euo pipefail
          URL="https://www.elmercadodeorigen.com/?pinterest_verify=${GITHUB_RUN_ID}"
          HTML="$(curl -fsSL --max-time 30 "$URL")"
          printf '%s' "$HTML" | grep -Fq '<meta name="p:domain_verify" content="bd9b4a2c373b774bb7753fa7040172ac">'
          echo 'pinterest_verify_ok'
